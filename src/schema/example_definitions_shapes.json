{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Defintion Schema",
    "description": "JSON schema for a scientific unit of measure",
    "type":"object",
    "definitions": {
        "shape_type":{
            "type":"string",
            "enum":[
                "Round",
                "Square?!?"
            ]
        }
    },
    "properties": {
        "shape": {"$ref": "#/definitions/shape_type"}
    }
 }